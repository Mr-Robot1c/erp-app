/** Cột đọc chung cho danh sách/chi tiết chứng từ — để ở module KHÔNG "use client" vì trang server cũng import. */
export const DOC_COLUMNS = "id, doc_no, doc_type, status, doc_date, partner_id, created_by, created_by_name, meta";
